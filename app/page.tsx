import Image from "next/image";

/**
 * Root page component that renders a centered full-screen wrapper with light and dark background styles.
 *
 * @returns A JSX element containing a full-height, centered container div with base sans-serif font and responsive background styling.
 */
export default function Home() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-zinc-50 font-sans dark:bg-black">
      
    </div>
  );
}